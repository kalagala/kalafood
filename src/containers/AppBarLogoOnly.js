import React from "react";
import Logo from "../components/Logo";
const AppBarLogoOnly = () => {
  return (
    <div className="app-bar">
      <Logo />
    </div>
  );
};

export default AppBarLogoOnly;
